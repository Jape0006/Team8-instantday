---
const { href, class: className = "" } = Astro.props;
---

<a
  href={href}
  class={`inline-block 
          bg-[var(--color-accent-900)] 
          text-black underline font-bold 
          py-2 px-6 rounded 
          hover:opacity-90 transition 
          text-sm sm:text-base md:text-base 
         
          ${className}`}
>
  <span class="text-center">
    <slot />
    <!-- her kommer teksten ind -->
  </span>
</a>
